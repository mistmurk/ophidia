const Discord = require('discord.js');
const config = require('./config/config.json');

module.exports = async (client, message) => {
  // Ingnore messages from bots
  if (message.author.bot) return;

  // TODO: Check for card text 
  
}
